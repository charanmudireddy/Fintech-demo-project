apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-project-api.fullname" . }}-test-connection"
  labels:
    {{- include "demo-project-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-project-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
